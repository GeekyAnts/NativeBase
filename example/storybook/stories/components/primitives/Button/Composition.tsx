import React from 'react';
import { Button } from 'native-base';

export default function () {
  return (
    <Button
      size="md"
      height={12}
      width={48}
      borderWidth={1}
      borderColor="green.500"
    >
      BUTTON
    </Button>
  );
}
